NODE_ENV=development
PORT=3000
TZ=Africa/Johannesburg

# Replace with your local Postgres connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/uct_residence?schema=public"

# JWT secrets (change in production and store in secret manager)
JWT_ACCESS_SECRET="change-me-access"
JWT_REFRESH_SECRET="change-me-refresh"
JWT_ACCESS_TTL="15m"
JWT_REFRESH_TTL="7d"

# SMTP settings (configure for production)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM="no-reply@example.com"
